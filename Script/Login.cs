using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class Login : MonoBehaviour {
    

    GameObject ID = GameObject.Find("IDInput");
    GameObject PW = GameObject.Find("");



	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
/*    void OnGUI()
    {
        GUI.Label(new Rect(Screen.width / 4, Screen.height / 4, 100, 100), ID_Value);
        GUI.Label(new Rect(Screen.width / 4, Screen.height / 4, 100, 100), PW_Value);
    }*/
}
